node_modules/
auth/
logs/
media/
.env
*.log

# Runtime/generated
STATUS.md
archive/

# Per-chat runtime data (backed up nightly, not in git)
data/*.json
data/*/context.json

# Keep per-chat memories in git (they're the learned knowledge)
!data/*/memory.md
skills/prompt-guard/prompt_guard/__pycache__/
.overlord-db-pass
