# Fail2ban filter for Traefik — excessive 4xx errors (brute force / scanning)
# Bans IPs that generate too many 400-499 responses in a short window

[Definition]

failregex = ^<HOST> \- \S+ \[\] \"[^\"]*\" 4\d\d\b

ignoreregex =
